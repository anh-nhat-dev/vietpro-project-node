 <div class="col-lg-12 col-md-12 col-sm-12">
  <nav>
    <div id="menu" class="collapse navbar-collapse">
      <ul>
        <% for (let cate of cates) { %>
        <li class="menu-item"><a href="/category-<%= cate.slug %>.<%= cate.id %>"><%= cate.title %></a></li>
      <% } %>
      </ul>
    </div>
  </nav>
</div>